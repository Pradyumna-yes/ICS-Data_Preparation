
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error Results - Fix Data</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .table-hover tbody tr:hover { background-color: #f5f5f5; }
        .correction-input:disabled { background-color: #e9ecef; opacity: 1; }
        .is-invalid { border-color: #dc3545; }
        .help-icon { 
            cursor: help;
            color: #17a2b8;
            font-size: 0.9em;
            margin-left: 5px;
        }
        .tooltip-inner {
            max-width: 300px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h1 class="mb-4">Error Results - Fix Data</h1>
        
        <form method="POST" action="{{ url_for('apply_fixes') }}" id="correction-form">
            <input type="hidden" name="file_id" value="{{ file_id }}">
            
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Type</th>
                            <th>Row</th>
                            <th>Field</th>
                            <th>Current Value</th>
                            <th>Corrected Value</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for error in errors %}
                        <tr>
                            <td>{{ error.type }}</td>
                            <td>{{ error.row }}</td>
                            <td>
                                {{ error.field }}
                                <span class="help-icon" 
                                      data-toggle="tooltip" 
                                      data-html="true"
                                      title="{{ error.tip }}">
                                    ⓘ
                                </span>
                            </td>
                            <td>{{ error.current_value }}</td>
                            <td>
                                <input type="text" 
                                       name="fix_{{ error.row }}_{{ error.field }}" 
                                       value="{{ error.current_value }}"
                                       class="form-control correction-input">
                            </td>
                            <td>
                                <button type="button" 
                                        class="btn btn-sm btn-outline-danger ignore-btn"
                                        data-row="{{ error.row }}" 
                                        data-field="{{ error.field }}">
                                    Ignore
                                </button>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            
            <div class="mt-4">
                <button type="submit" class="btn btn-success btn-lg">
                    Apply Fixes & Download
                </button>
                <a href="{{ url_for('download_file', filename=output_file) }}"
                   class="btn btn-secondary btn-lg">
                    Download Original
                </a>
            </div>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    
    <script>
        $(document).ready(function(){
            // Initialize tooltips
            $('[data-toggle="tooltip"]').tooltip();

            // Ignore button handler
            $('.ignore-btn').click(function() {
                const row = $(this).data('row');
                const field = $(this).data('field');
                const input = $(`input[name="fix_${row}_${field}"]`);
                
                input.prop('disabled', !input.prop('disabled'));
                $(this).toggleClass('btn-danger btn-outline-danger')
                       .text(input.prop('disabled') ? 'Undo Ignore' : 'Ignore');
            });

            // Form validation
            $('#correction-form').submit(function(e) {
                let valid = true;
                $('.correction-input').each(function() {
                    if (!$(this).prop('disabled') && !$(this).val().trim()) {
                        valid = false;
                        $(this).addClass('is-invalid');
                    } else {
                        $(this).removeClass('is-invalid');
                    }
                });
                
                if (!valid) {
                    e.preventDefault();
                    alert('Please fill in all required corrections or mark them as ignored!');
                }
            });
        });
    </script>
</body>
</html>












